channels:
  web:
    # keep empty => use all tools
    allow_tools: []
    deny_tools: []
  feishu:
    # example: disallow potentially risky tools in IM channels
    allow_tools: []
    deny_tools:
      - bash

