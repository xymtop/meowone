"""编排层说明（非运行时依赖，仅作文档锚点）。

当前实现
--------
远程「智能体」通过 A2A 接入后，在 MeowOne 里统一注册为 **OpenAI 风格的 function tools**。
主循环在 `runtime.run_loop` 中：模型可在 **同一轮 assistant 消息** 里返回多个 `tool_calls`，
服务端用 `asyncio.gather` **并行**执行各工具，再把多条 `tool` 角色消息按 `tool_call_id` 写回上下文。

这样做好处
----------
- 与现有 LLM 生态一致，无需自研 RPC 协议即可替换/增加模型。
- 并行度由「模型是否在同一轮发起多工具」决定；执行侧已支持真并行。

若未来要扩展
------------
- **DAG / 条件分支**：可引入独立 `Scheduler`（读取计划图再按阶段调用工具），与「工具定义」正交；
- **多主智能体辩论**：可在 scheduler 之上再包一层，不强制推翻 tool 注册方式。
- 因此「智能体映射为工具」是务实默认；复杂编排用 **额外调度器** 扩展即可，不必绑死一种方案。
"""
