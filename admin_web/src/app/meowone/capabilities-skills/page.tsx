"use client";
import { useEffect, useState } from "react";
import { meowoneApi, type SkillsListResponse } from "@/lib/meowone-api";
export default function CapabilitiesSkillsPage(){
  const [data,setData]=useState<SkillsListResponse|null>(null); const [error,setError]=useState(""); const [name,setName]=useState(""); const [description,setDescription]=useState(""); const [body,setBody]=useState("");
  const load=async()=>{try{setError(""); setData(await meowoneApi.listSkills());}catch(e){setError((e as Error).message);}}; useEffect(()=>{void load();},[]);
  return (<div className="space-y-6"><h1 className="text-title-md2 font-semibold text-dark dark:text-white">技能管理</h1>{error?<p className="text-sm text-red">{error}</p>:null}<div className="rounded-xl border border-stroke bg-white p-4 dark:border-dark-3 dark:bg-dark-2"><h2 className="mb-3 text-sm font-semibold">新增 / 更新</h2><input value={name} onChange={(e)=>setName(e.target.value)} placeholder="名称" className="mb-2 w-full rounded-lg border border-stroke px-3 py-2 text-sm dark:border-dark-3"/><input value={description} onChange={(e)=>setDescription(e.target.value)} placeholder="描述" className="mb-2 w-full rounded-lg border border-stroke px-3 py-2 text-sm dark:border-dark-3"/><textarea value={body} onChange={(e)=>setBody(e.target.value)} placeholder="内容（Markdown）" className="h-40 w-full rounded-lg border border-stroke px-3 py-2 text-sm dark:border-dark-3"/><button className="mt-3 rounded-lg bg-primary px-4 py-2 text-sm text-white" onClick={async()=>{await meowoneApi.upsertSkill({name,description,body});await load();}}>提交</button></div><div className="space-y-2">{(data?.skills||[]).map((s)=><div key={s.name} className="flex items-start justify-between rounded-xl border border-stroke bg-white p-3 dark:border-dark-3 dark:bg-dark-2"><div><p className="font-medium">{s.name}</p><p className="text-xs text-body dark:text-dark-6">{s.description}</p></div><button className="text-sm text-red" onClick={async()=>{if(!confirm(`删除技能 ${s.name}?`))return;await meowoneApi.deleteSkill(s.name);await load();}}>删除</button></div>)}</div></div>);
}
