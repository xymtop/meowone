"use client";
import { useEffect, useState } from "react";
import { meowoneApi, type MenusListResponse } from "@/lib/meowone-api";
export default function MenusPage(){
  const [flat,setFlat]=useState(false); const [res,setRes]=useState<MenusListResponse|null>(null); const [error,setError]=useState("");
  const [menuKey,setMenuKey]=useState(""); const [name,setName]=useState(""); const [path,setPath]=useState(""); const [component,setComponent]=useState(""); const [icon,setIcon]=useState(""); const [parentKey,setParentKey]=useState(""); const [sort,setSort]=useState("0"); const [visible,setVisible]=useState(true); const [enabled,setEnabled]=useState(true); const [metaJson,setMetaJson]=useState("{}"); const [deleteKey,setDeleteKey]=useState("");
  const load=async()=>{try{setError("");setRes(await meowoneApi.listMenus(flat));}catch(e){setError((e as Error).message);}};
  useEffect(()=>{void load();},[flat]);
  return (<div className="space-y-6"><h1 className="text-title-md2 font-semibold text-dark dark:text-white">菜单管理</h1><label className="flex items-center gap-2 text-sm"><input type="checkbox" checked={flat} onChange={(e)=>setFlat(e.target.checked)}/>平铺模式（flat=true）</label>{error?<p className="text-sm text-red">{error}</p>:null}<pre className="max-h-64 overflow-auto rounded-xl border border-stroke bg-white p-3 text-xs dark:border-dark-3 dark:bg-dark-2">{JSON.stringify(res?.menus??null,null,2)}</pre><div className="rounded-xl border border-stroke bg-white p-4 dark:border-dark-3 dark:bg-dark-2"><h2 className="mb-3 text-sm font-semibold">新增 / 更新</h2><div className="grid grid-cols-1 gap-2 md:grid-cols-2"><input value={menuKey} onChange={(e)=>setMenuKey(e.target.value)} placeholder="menu_key" className="rounded-lg border border-stroke px-3 py-2 text-sm dark:border-dark-3"/><input value={name} onChange={(e)=>setName(e.target.value)} placeholder="名称" className="rounded-lg border border-stroke px-3 py-2 text-sm dark:border-dark-3"/><input value={path} onChange={(e)=>setPath(e.target.value)} placeholder="path" className="rounded-lg border border-stroke px-3 py-2 text-sm dark:border-dark-3"/><input value={component} onChange={(e)=>setComponent(e.target.value)} placeholder="component" className="rounded-lg border border-stroke px-3 py-2 text-sm dark:border-dark-3"/><input value={icon} onChange={(e)=>setIcon(e.target.value)} placeholder="icon" className="rounded-lg border border-stroke px-3 py-2 text-sm dark:border-dark-3"/><input value={parentKey} onChange={(e)=>setParentKey(e.target.value)} placeholder="parent_key（可选）" className="rounded-lg border border-stroke px-3 py-2 text-sm dark:border-dark-3"/><input value={sort} onChange={(e)=>setSort(e.target.value)} placeholder="sort" className="rounded-lg border border-stroke px-3 py-2 text-sm dark:border-dark-3"/><label className="flex items-center gap-2 text-sm"><input type="checkbox" checked={visible} onChange={(e)=>setVisible(e.target.checked)}/>可见</label><label className="flex items-center gap-2 text-sm"><input type="checkbox" checked={enabled} onChange={(e)=>setEnabled(e.target.checked)}/>启用</label></div><textarea value={metaJson} onChange={(e)=>setMetaJson(e.target.value)} placeholder="meta JSON" className="mt-2 h-24 w-full rounded-lg border border-stroke px-3 py-2 font-mono text-xs dark:border-dark-3"/><button className="mt-3 rounded-lg bg-primary px-4 py-2 text-sm text-white" onClick={async()=>{let meta={}; try{meta=JSON.parse(metaJson||"{}");}catch{setError("meta JSON 无效");return;} await meowoneApi.upsertMenu({menu_key:menuKey,name,path,component,icon,parent_key:parentKey||null,sort:Number(sort)||0,visible,enabled,meta}); await load();}}>提交</button></div><div className="flex flex-wrap items-end gap-2 rounded-xl border border-stroke bg-white p-4 dark:border-dark-3 dark:bg-dark-2"><input value={deleteKey} onChange={(e)=>setDeleteKey(e.target.value)} placeholder="要删除的 menu_key" className="rounded-lg border border-stroke px-3 py-2 text-sm dark:border-dark-3"/><button className="rounded-lg border border-red px-4 py-2 text-sm text-red" onClick={async()=>{if(!deleteKey.trim())return; if(!confirm(`删除菜单 ${deleteKey}?`))return; await meowoneApi.deleteMenu(deleteKey.trim()); setDeleteKey(""); await load();}}>删除</button></div></div>);
}
